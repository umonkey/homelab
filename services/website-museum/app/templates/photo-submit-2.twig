{% extends "base.twig" %}

{% block page_title %}Добавление фотографии — {{ site_name }}{% endblock %}

{% block content %}
    {% include "parts/breadcrumbs.twig" %}

    <div class="page-header">
        <h1>Пополнение коллекции старых фотографий</h1>
    </div>

    <p>Выделите область фотографии, которую нужно выводить на сайте.  Позже эту область можно будет изменить.</p>

    <form method="post" class="async">
        <input type="hidden" name="step" value="2"/>
        <input type="hidden" name="crop_data" value="" id="crop_data"/>

        <div class="photo" style="margin: 20px 0">
            {% set i = photo.images.old.o %}
            <img class="cropme" src="{{ i.link }}" data-storage="#crop_data" data-width="{{ i.width }}" data-height="{{ i.height }}"/>
        </div>

        <div class="form-group actions">
            <button class="btn btn-primary">Продолжить</button>
        </div>

        <div class="progressbar" style="display:none"><div class="done"></div></div>
    </form>
{% endblock %}
